/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    trailingSlash: false,
    reactStrictMode: true,
    target: 'serverless',
};

module.exports = nextConfig;
